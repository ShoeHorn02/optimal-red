{
  "name": "optimal-red",
  "version": "0.0.1",
  "description": "Optimal Red - Apple Health tracking platform for Watch, iPhone, Mac, and Web",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/backend",
    "build": "npm run build --workspace=packages/backend",
    "test": "npm run test --workspace=packages/backend && npm run test --workspace=packages/shared",
    "lint": "npm run lint --workspace=packages/backend"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
